# Manifest

Package: `axiom_triz_full_docs`

Language: Russian with DSL terms in English

Purpose:

```text
Читаемая документация нового Axiom TRIZ-подхода:
пользовательский DSL, normalizer, runtime mapping, Go boundary,
Rule Studio, HydroPilot and production path.
```

Scope:

- не заменяет текущую Axiom v0 спецификацию из `axiom-main/docs`;
- описывает целевой user-facing слой;
- фиксирует соответствие между новым подходом и реализованным runtime;
- оставляет лишнюю runtime-механику за пределами пользовательской модели.
